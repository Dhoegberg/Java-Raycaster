package debug;

public class PrintGrid {
	/*clientMapGrid = new MapGrid(10, 10);
	clientMapGrid.makeOpenRoom();
	
	StringBuffer room = new StringBuffer(100);
	for(int x = 0; x < clientMapGrid.getSizeX(); x++){
		StringBuffer row = new StringBuffer(clientMapGrid.getSizeX());
		for(int y = 0; y < clientMapGrid.getSizeY(); y++){
			byte tile = clientMapGrid.getByteAtPos(x, y);
			if(tile == TileType.OPEN.getByteValue()) row.append(".  ");
			if(tile == TileType.WALL.getByteValue()) row.append('X');
		}
		room.append(row + "\n");
	}
	JOptionPane.showMessageDialog(null, room);*/
}
